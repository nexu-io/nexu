name: DB Migrate (MVP)

on:
  push:
    branches: ["**"]

permissions:
  contents: read

concurrency:
  group: db-migrate-mvp-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  migrate-apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect migration SQL and print manifest (debug)
        shell: bash
        env:
          DB_MIGRATION_BASE_REF: main
        run: |
          set -euo pipefail

          HEAD_SHA="${{ github.sha }}"
          BASE_REF="${DB_MIGRATION_BASE_REF}"

          git fetch --no-tags origin "+refs/heads/${BASE_REF}:refs/remotes/origin/${BASE_REF}"
          BASE_SHA="$(git merge-base "refs/remotes/origin/${BASE_REF}" "${HEAD_SHA}")"

          echo "Diff baseline branch: origin/${BASE_REF}"
          echo "Diff range: ${BASE_SHA}...${HEAD_SHA}"

          mapfile -t changed_files < <(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}" -- "apps/api/migrations" | sort -u)

          sql_files=()
          for file in "${changed_files[@]}"; do
            if [[ "$file" == *.sql && -f "$file" ]]; then
              sql_files+=("$file")
            fi
          done

          if [[ ${#sql_files[@]} -eq 0 ]]; then
            echo "No changed migration SQL files found against origin/${BASE_REF}. Skip."
            exit 0
          fi

          printf '%s\n' "${sql_files[@]}" > "$RUNNER_TEMP/migration-files.txt"

          manifest_path="$RUNNER_TEMP/nexu-db-migration-manifest.json"
          MIGRATION_FILE_LIST="$RUNNER_TEMP/migration-files.txt" MANIFEST_PATH="$manifest_path" node <<'NODE'
          const crypto = require("node:crypto");
          const fs = require("node:fs");

          const listPath = process.env.MIGRATION_FILE_LIST;
          const manifestPath = process.env.MANIFEST_PATH;

          if (!listPath || !manifestPath) {
            throw new Error("Missing required environment variables for manifest generation.");
          }

          const files = fs
            .readFileSync(listPath, "utf8")
            .split("\n")
            .map((entry) => entry.trim())
            .filter((entry) => entry.length > 0)
            .sort();

          const manifest = {
            repository: process.env.GITHUB_REPOSITORY,
            ref: process.env.GITHUB_REF,
            sha: process.env.GITHUB_SHA,
            runId: process.env.GITHUB_RUN_ID,
            eventName: process.env.GITHUB_EVENT_NAME,
            generatedAt: new Date().toISOString(),
            files: files.map((filePath) => {
              const sql = fs.readFileSync(filePath, "utf8");
              const sha256 = crypto.createHash("sha256").update(sql).digest("hex");
              return {
                path: filePath,
                sha256,
                sql,
              };
            }),
          };

          fs.writeFileSync(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`, "utf8");
          NODE

          echo "Detected migration files:"
          printf ' - %s\n' "${sql_files[@]}"

          echo "Manifest path: ${manifest_path}"
          echo "----- manifest.json begin -----"
          cat "${manifest_path}"
          echo "----- manifest.json end -----"

          echo "Debug mode: manifest generated only; CLI call skipped."
